SELECT w1.Id
FROM Weather w1, Weather w2
WHERE w1.Temperature > w2.Temperature
AND DATEDIFF(w1.RecordDate, w2.RecordDate) = 1

-----
Interval comparing, use self join 
Date difference - DATEDIFF() function
